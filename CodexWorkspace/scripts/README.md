# Script Templates

Reusable starting points for new tools and quick validations.

- `new_command_template.py` — minimal pyRevit command skeleton (explicit transaction, collector patterns).
- `align_move_geometry_harness.py` — lightweight geometry smoke checks for the Align+Move tool helpers.

